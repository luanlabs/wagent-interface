// import styled from 'styled-components';

// interface IArrowStyle {
//   placement?: 'top' | 'bottom';
// }

// export const ArrowStyle = styled.div<IArrowStyle>`
//   .tippy-svg-arrow {
//     transform: ${(props) => (props.placement === 'bottom' ? 'none' : 'rotateX(180deg)')} !important;
//     width: 100%;
//     display: flex;
//     justify-content: ${(props) => (props.placement === 'bottom' ? 'end' : 'center')};
//     fill: #050142;
//     margin-top: -1px;
//     margin-left: ${(props) => (props.placement === 'bottom' ? '-8px' : '3px')} !important;
//     top: ${(props) => (props.placement === 'bottom' ? '-5px' : 'none')};
//   }

//   .tippy-box {
//     line-height: 0.9em;
//   }
// `;
